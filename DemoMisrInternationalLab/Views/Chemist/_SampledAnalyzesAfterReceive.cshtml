@model List<DemoMisrInternationalLab.Models.PatientRequestAnalysi>
@if (Model != null)
{
    for (int i = 0; i < Model.Count; i++)
    {
        <div class="ck-buttonb">
            <label>

                <span>
                    <table width="100%">

                        <tr>
                            <td width="8%">@Html.DisplayTextFor(m => Model[i].PatientRequest.RequestNumber)</td>
                            <td width="82%">@Html.DisplayFor(x => Model[i].Analysis.AnalysisName)</td>
                            <td width="10%"></td>
                        </tr>

                    </table>
                </span>
            </label>
        </div>
    }
}
